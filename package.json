{
  "name": "template-test",
  "version": "1.0.0",
  "description": "Build pipeline for ReSpec + Puppeteer",
  "private": true,
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "puppeteer": "^22.7.1",
    "http-server": "^14.1.1"
  }
}
